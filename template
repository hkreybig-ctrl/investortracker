```html
<!doctype html>
<html>
<head>
  <meta charset="utf-8"/>
  <meta name="viewport" content="width=device-width,initial-scale=1"/>
  <title>Investment Tracker — Real-time PTR & 13F</title>
  <link rel="stylesheet" href="/static/styles.css">
</head>
<body>
  <header class="site-header">
    <h1>Investment Tracker</h1>
    <p class="subtitle">Real-time public filings from lawmakers & top investors — updated live.</p>
  </header>

  <main class="container">
    <section class="panel">
      <h2>Featured People</h2>
      <form id="peopleForm" action="/set_people" method="post">
        <div class="people-grid">
          {% for p in featured %}
            <label class="person-card">
              <input type="checkbox" name="selected" value="{{ p }}" {% if p in selected %}checked{% endif %}>
              <div class="person-name">{{ p }}</div>
            </label>
          {% endfor %}
        </div>
        <div style="margin-top:12px;">
          <button type="submit" class="btn">Follow selection</button>
          <button type="button" class="btn btn-secondary" id="clearBtn">Clear</button>
        </div>
      </form>
    </section>

    <section class="panel">
      <h2>Live Dashboard</h2>
      <div id="statusBar" class="status">Connecting to live feed…</div>
      <table id="filingsTable" class="filings">
        <thead>
          <tr>
            <th>Date</th>
            <th>Person</th>
            <th>Ticker</th>
            <th>Side</th>
            <th>Amount</th>
            <th>Source</th>
            <th>Action</th>
          </tr>
        </thead>
        <tbody>
          {% for row in rows %}
          <tr>
            <td>{{ row[4] or "" }}</td>
            <td>{{ row[0] or "" }}</td>
            <td>{{ row[1] or "" }}</td>
            <td>{{ row[2] or "" }}</td>
            <td>{{ row[3] or "" }}</td>
            <td>{{ row[5] or "" }}</td>
            <td><button class="btn-mini" onclick="replicate('{{ row[1] }}')">Replicate</button></td>
          </tr>
          {% else %}
          <tr><td colspan="7">No filings yet.</td></tr>
          {% endfor %}
        </tbody>
      </table>
    </section>

    <section class="panel">
      <h2>Disclaimer</h2>
      <p class="muted">This site displays public filings and information for educational purposes only. Nothing here is financial advice. Always do your own research.</p>
    </section>
  </main>

  <footer class="site-footer">
    <small>Built with public APIs. Adjust polling interval in configuration.</small>
  </footer>

  <script src="/static/app.js"></script>
</body>
</html>
